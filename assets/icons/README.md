# Icon Placeholder Notes

This folder contains placeholder PNG icons used by the extension:

- `icon16.png`
- `icon32.png`
- `icon48.png`
- `icon128.png`

Current files are generated placeholders with a blue background and `D` letter.

For production release, replace them with your official brand icons using the same filenames and sizes.
